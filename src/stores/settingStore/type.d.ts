import { SettingStore as SettingStoreModel } from './index';

/**
 * 声明ISettingStore为包含子属性的对象
 * 全局定义并输出ISettingStore
 */
export declare namespace ISettingStore {
  // 声明并输出SettingStore为包含子属性的对象模型
  export interface SettingStore extends SettingStoreModel {}
}
