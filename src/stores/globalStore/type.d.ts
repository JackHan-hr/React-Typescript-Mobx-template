import { GlobalStore as GlobalStoreModel } from './index';

/**
 * 声明IGlobalStore为包含子属性的对象
 * 全局定义并输出IGlobalStore
 */
export declare namespace IGlobalStore {
  // 声明并输出GlobalStore为包含子属性的对象模型
  export interface GlobalStore extends GlobalStoreModel {}
}
